##Cypress Project

This project uses Cypress for end-to-end testing. The following description will guide you through the project details

##Project Structure

fixtures : Contains test data files
cypress/e2e: Contains test spec files
support : Contains page objects

##Environments

In cypress.config.js :
baseUrl: "https://demoqa.com/"
env.apiUrl: "https://reqres.in/api" (for testing API calls )

##Running Tests

Install the dependencies: npm install
Open Cypress Test Runner: npx cypress open - for running tests select firefox browser
Run the tests: npx cypress run --browser firefox
